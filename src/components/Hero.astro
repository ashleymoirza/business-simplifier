<section class="relative overflow-hidden bg-gradient-to-br from-brand-950 via-brand-900 to-brand-800">
  {/* Modern tech grid background */}
  <div class="absolute inset-0 opacity-[0.03]" style="background-image: linear-gradient(to right, #fff 1px, transparent 1px), linear-gradient(to bottom, #fff 1px, transparent 1px); background-size: 60px 60px;" />
  <div class="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l from-accent-500/5 to-transparent" />

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20 sm:py-28 lg:py-36">
    <div class="max-w-3xl">
      {/* Badge */}
      <div class="inline-flex items-center gap-2 px-3 py-1 mb-6 text-xs font-semibold text-accent-300 bg-accent-900/30 rounded-full border border-accent-700/30">
        <span class="w-1.5 h-1.5 bg-accent-400 rounded-full animate-pulse-subtle"></span>
        Free 1-Hour Consultation — No Commitment
      </div>

      {/* Headline with actual data */}
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-white leading-tight tracking-tight">
        <span class="block">Save <span class="text-accent-400">R135K/Year</span></span>
        <span class="block text-transparent bg-clip-text bg-gradient-to-r from-accent-300 to-accent-500">
          By Simplifying With AI
        </span>
      </h1>

      {/* Data-backed subtext */}
      <p class="mt-6 text-lg sm:text-xl text-brand-100/80 max-w-2xl leading-relaxed">
        Based on real data: businesses using AI save <strong class="text-white">R9K–R36K/month</strong> and free up <strong class="text-white">20+ hours/month</strong> in team time. We help SMEs automate repetitive work, cut costs, and scale smarter.
      </p>

      {/* CTAs */}
      <div class="mt-8 flex flex-col sm:flex-row gap-4">
        <a
          href="/contact"
          class="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-brand-950 bg-accent-400 hover:bg-accent-300 rounded-lg transition-colors shadow-lg hover:shadow-xl hover:scale-[1.02] active:scale-[0.98]"
        >
          Free 1-Hour Consultation
          <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
        </a>
        <a
          href="/services"
          class="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-white border-2 border-brand-400/50 hover:border-brand-400 hover:bg-brand-800/50 rounded-lg transition-colors backdrop-blur-sm"
        >
          View Services
        </a>
      </div>

      {/* Trust indicators with data */}
      <div class="mt-12 grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div class="bg-white/5 backdrop-blur-sm rounded-lg px-4 py-3 border border-white/5">
          <p class="text-accent-400 text-lg font-bold">R135K</p>
          <p class="text-brand-200/60 text-xs">Average annual savings</p>
        </div>
        <div class="bg-white/5 backdrop-blur-sm rounded-lg px-4 py-3 border border-white/5">
          <p class="text-accent-400 text-lg font-bold">20+ hrs</p>
          <p class="text-brand-200/60 text-xs">Time saved per month</p>
        </div>
        <div class="bg-white/5 backdrop-blur-sm rounded-lg px-4 py-3 border border-white/5">
          <p class="text-accent-400 text-lg font-bold">4-7x</p>
          <p class="text-brand-200/60 text-xs">Average ROI on AI spend</p>
        </div>
      </div>
    </div>
  </div>
</section>